- defaults:
    link:
        create: true
        force: true
- link:
    # Window manager
    ~/.config/sway: sway
    ~/.config/swaylock: swaylock
    ~/Pictures/Wallpapers: wallpapers
    ~/.config/waybar: waybar
    # Shell
    ~/.config/fish: fish
    # Applications
    ~/.config/nvim: nvim
    ~/.config/git: git
    ~/.config/alacritty: alacritty
    ~/.config/ranger: ranger
    ~/.config/tealdeer: tealdeer
    ~/.config/zathura: zathura
    ~/.proxychains: proxychains
    ~/.config/mako: mako
    ~/.config/ulauncher: ulauncher
    ~/.config/nyxt : nyxt
- shell:
    - [rm -rf ~/.aria2]
    - [git clone https://github.com/P3TERX/aria2.conf.git ~/.aria2]
    - [touch ~/.aria2/aria2.session]
- link:
    ~/.aria2/aria2.conf:
      path: aria2/aria2.conf
    # Developments
    ~/.cargo/config.toml: cargo/config.toml
    ~/.roswell/init.lisp: roswell/init.lisp
    # Scripts
    ~/Scripts: scripts
    # Electron
    ~/.config/electron-flags.conf: electron/electron-flags.conf
    ~/.config/electron12-flags.conf: electron/electron-flags.conf
    ~/.config/electron13-flags.conf: electron/electron-flags.conf
    ~/.config/chrome-flags.conf: electron/electron-flags.conf
    ~/.config/chromium-flags.conf: electron/electron-flags.conf
    ~/.inputrc: readline/.inputrc
    ~/.config/kitty: kitty
    ~/.zshenv: zsh/.zshenv.home
    ~/.config/zsh: zsh
    ~/.config/starship: starship
    ~/.config/bat: bat
    ~/.config/kmonad: kmonad
- shell:
    - [rm -rf ~/.config/kitty/kitty-themes]
    - [git clone --depth 1 https://github.com/dexpota/kitty-themes.git ~/.config/kitty/kitty-themes]
    - [rm -rf ~/.config/kitty/theme.conf && ln -s ~/.config/kitty/kitty-themes/themes/Apprentice.conf ~/.config/kitty/theme.conf]
