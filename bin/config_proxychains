#/usr/bin/env bash

HOST_IP="$(cat /etc/resolv.conf | grep nameserver | awk '{ print $2 }')"

mkdir -p ~/.proxychains

cat <<EOF > ~/.proxychains/proxychains.conf
# File generated by config_proxychains
[ProxyList]
socks5 	${HOST_IP} 9050
EOF
